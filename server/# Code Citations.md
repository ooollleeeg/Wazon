# Code Citations

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````

## License: unknown

https://github.com/sparkbox/exploring-cohort-api/blob/1f7e351df211196deed3e5801f4362b58de07cae/src/services/db.js

`````
Проблема в том, что таблица `personnel` в БД не имеет колонок `officialRank` и `actualRank`. Нужно обновить БД.

## 🔧 Решение: Пересоздать таблицу personnel

Удалите старую БД и пересоздайте её с правильной схемой.

### Вариант 1: Удалить файл БД (самый простой)

1. Закройте сервер (Ctrl+C)
2. Удалите файл: `data/oid_registry.db`
3. Запустите снова: `npm run dev`

Сервер автоматически пересоздаст БД с правильной схемой.

---

## Или Вариант 2: Обновить database.js с миграцией

Если у вас уже есть данные, которые нужно сохранить:

````javascript
// filepath: c:\Users\Администратор\OneDrive\Документи\GitHub\vazon\server\database.js
import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const dbPath = path.join(__dirname, '../data/oid_registry.db');

const
`````
